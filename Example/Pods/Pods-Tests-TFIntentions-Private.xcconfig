#include "Pods-Tests-TFIntentions.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/TFIntentions" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/TFIntentions"
OTHER_LDFLAGS = -ObjC
PODS_ROOT = ${SRCROOT}